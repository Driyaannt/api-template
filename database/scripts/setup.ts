import { copyFile, access } from 'node:fs/promises'; import { constants } from 'node:fs'; import { spawnSync } from 'node:child_process';
try{await access('.env',constants.F_OK)}catch{await copyFile('.env.example','.env');console.log('Created .env from .env.example. Update secrets and DATABASE_URL before continuing.')}const args=process.argv.slice(2);const accessMode=args.find(v=>v.startsWith('--access='))?.split('=')[1]??'raw';if(!['raw','prisma','sequelize'].includes(accessMode))throw new Error('Invalid --access value');console.log(`Selected DB_ACCESS_MODE=${accessMode}. Edit .env if it differs.`);if(accessMode!=='raw')console.log('This starter currently runs the raw adapter; Prisma and Sequelize scaffolding is documented for extension.');const result=spawnSync('pnpm',['db:migrate'],{stdio:'inherit',shell:true});if(result.status!==0)process.exit(result.status??1);
